# Long Code Lines

```javascript
const veryLongLine = "mdv keeps long code lines horizontally scrollable until the per-block wrap button is enabled, and this intentionally lengthy JavaScript string is here to force overflow in the regression harness at desktop width.";
```
